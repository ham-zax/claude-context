# Archived ranking evaluation corpus

The former cross-repository ranking manifests, policies, task suites, and
evaluation builders live here as historical records only. They are not release
gates, runtime dependencies, or inputs to native reranker ordering.
